---
# .deployed sibling for the RETIRED (v1) automation pair on Arbitrum, superseded when
# `just deploy-automation` deployed the v2 pair under the Automation Owner.
#
# Hand-written and committed (NOT generated by `just deploy-automation`): once a pair is retired its
# addresses are frozen history. Same v1 trigger/receiver addresses as the other three lanes (CREATE2);
# Arbitrum's CustomSender differs. See l2-optimism-extras.deployed.yaml for the full rationale.
deployed:
  l1: [] # L2-only config; the L1 receiver is verified by l1.yaml.
  l2:
    - &l2CustomSender "0x72229141D4B016682d3618ECe47c046f30Da4AD1"
    - &l2RetiredSyncTrigger "0x1594705D5f9BbDb36453ACF15C94d041c0E02c62"
    - &l2RetiredCreReceiver "0x29113eD7AE4C97Ee2F20A5511C852aa37C0d6b85"
