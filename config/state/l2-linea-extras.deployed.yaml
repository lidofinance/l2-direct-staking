---
# STATIC, hand-authored sibling of l2-linea-extras.yaml — NOT generated by a deploy. Holds only the
# pre-existing CustomSender proxy address (deterministic; a deploy input, not a Stage-1 output) that
# the Linea-only Gelato de-role check is asserted against.
deployed:
  l1: [] # no L1 contracts checked here
  l2:
    - &l2CustomSender "0x328de900860816d29D1367F6903a24D8ed40C997"
