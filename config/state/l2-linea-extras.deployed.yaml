---
# Static .deployed sibling of l2-linea-extras.yaml. UNLIKE the per-lane l2-<net>.deployed.yaml files
# (generated by 'just deploy-stage1' from each fork's fresh deploy outputs), this one is hand-written
# and committed: it pins pre-existing contracts the Linea-only extras config reads — the upstream
# CustomSenderReferral proxy and the retired (v1) automation pair (same CREATE2 addresses as the
# other three lanes). Pinned to LineaMigrationConstants.L2_CUSTOM_SENDER by
# 'just verify-constants-sync'. This file supplies the schema-required `deployed:` section and
# resolves the *l2CustomSender / *l2Retired* aliases in the wiring config.
deployed:
  l1: [] # L2-only config; the L1 receiver is verified by l1.yaml.
  l2:
    - &l2CustomSender "0x328de900860816d29D1367F6903a24D8ed40C997"
    - &l2RetiredSyncTrigger "0x1594705D5f9BbDb36453ACF15C94d041c0E02c62"
    - &l2RetiredCreReceiver "0x29113eD7AE4C97Ee2F20A5511C852aa37C0d6b85"
